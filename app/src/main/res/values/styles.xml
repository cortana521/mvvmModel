<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <!--        <item name="android:windowBackground">@mipmap/app_welcome_bg</item>-->
        <!--list分割线颜色-->
        <item name="android:listDivider">@drawable/shape_divider_bg</item>
    </style>

    <style name="AppTheme.NoActionBar.FullScreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@mipmap/app_welcome_bg</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ToolBar菜单样式 -->
    <style name="ToolbarTheme" parent="@style/ThemeOverlay.MaterialComponents.ActionBar">
        <item name="actionMenuTextColor">@color/menu_color</item>
        <item name="android:textSize">@dimen/text_dimen_16</item>
    </style>

    <style name="BottomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowAnimationStyle">@style/BottomSelectAnimation</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="BottomSelectAnimation" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/bottom_in_window</item>
        <item name="android:windowExitAnimation">@anim/bottom_out_window</item>
    </style>

    <!-- 加载dialog-->
    <style name="LoadingDialog" parent="@android:style/Theme.Holo.Dialog.NoActionBar">
        <item name="android:windowBackground">@drawable/toast_trans_bg</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <!--水平方向的一条分割线-->
    <style name="ViewLineHorizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_01</item>
        <item name="android:background">@color/divider_line_color</item>
    </style>

    <!--水平方向的一条分割线,细一些-->
    <style name="ViewLineHorizontalContent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_double_5</item>
        <item name="android:background">@color/divider_line_color</item>
    </style>
    <!--水平方向的一条分割线,细一些-->
    <style name="ViewLineHorizontalContent_10">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_10</item>
        <item name="android:background">@color/divider_line_color</item>
    </style>

    <!--竖直方向的一条分割线-->
    <style name="ViewLineVertical">
        <item name="android:layout_width">@dimen/dp_01</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/divider_line_color</item>
    </style>


    <!-- EditText下划线样式 -->
    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/editLineColor</item>
        <item name="colorControlActivated">@color/colorEditText</item>
    </style>

    <!--开方页面的竖直方向的一条分割线-->
    <style name="ViewLineVerticalInPrescription">
        <item name="android:layout_width">@dimen/dp_01</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/divider_line_color</item>
        <item name="android:layout_marginBottom">@dimen/dp_08</item>
        <item name="android:layout_marginTop">@dimen/dp_08</item>
    </style>
    <!--开方页面的竖直方向的一条分割线-->
    <style name="ViewLineVerticalInPrescriptions">
        <item name="android:layout_width">1px</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@color/divider_line_color</item>
        <item name="android:layout_marginBottom">@dimen/dp_16</item>
        <item name="android:layout_marginTop">@dimen/dp_16</item>
    </style>

    <style name="custom_dialog2" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.3</item>
    </style>

    自定义仿IOS的AlertDialog的样式

    <style name="AlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>

    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <!--仿京东地址选择样式-->
    <style name="bottom_dialog" parent="android:style/Theme.Dialog">
        <!-- 设置无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置未浮动窗口 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置完全透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/Animation_Bottom_Dialog</item>
    </style>

    <style name="Animation_Bottom_Dialog">
        <item name="android:windowEnterAnimation">@anim/bottom_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_dialog_exit</item>
    </style>

    <!--我的页面条目的样式-->
    <style name="item_stytle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/dp_45</item>
        <item name="android:textColor">@color/black3</item>
        <item name="android:textSize">@dimen/text_dimen_15</item>
        <item name="android:background">@color/white</item>
        <item name="android:paddingLeft">@dimen/dp_15</item>
        <item name="android:paddingRight">@dimen/dp_15</item>
        <item name="android:drawableRight">@mipmap/arrow_right</item>
        <item name="android:drawablePadding">@dimen/dp_10</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>